# 服务器部署网页

以下将演示三台服务器共同部署一个简单网页项目

服务器1：

ubuntu-24.04.3-desktop-amd64	4核4g	192.168.253.128	部署Java Spring boot

服务器2：

CentOS-Stream-10	3核4g	192.168.253.135	部署Mysql

服务器3：

CentOS-Stream-10 3核2g	192.168.253.136	部署Nginx+Redis

首先准备网页项目：system_zzj

项目由deepseek v4 pro harness编写

## windows 11运行

先放我windows 11 环境测试运行：

先进入加载Maven

### 加载数据库

执行sql

![](pic/Snipaste_2026-09-05_19-07-12.png)

### 运行后端

运行后端DemoApplication

发现报错

![](pic/Snipaste_2026-09-05_19-09-29.png)

实际是ai编写yml配置里的Mysql密码错误用了环境变量DB_PASSWORD=root未加载以及测试用的密码123456

把密码改成root就行

![](pic/Snipaste_2026-09-05_19-15-21.png)

或者idea加载环境变量

![](pic/Snipaste_2026-09-05_19-17-08.png)

选择编辑配置

![](pic/Snipaste_2026-09-05_19-18-42.png)

勾选环境变量，添加一条

DB_PASSWORD=root

![](pic/Snipaste_2026-09-05_19-20-40.png)

应用运行就行

后端运行成功

![](pic/Snipaste_2026-09-05_19-23-27.png)

### 运行前端

```vue
cd .\frontend\
npm install
npm run dev
```

![](pic/Snipaste_2026-09-05_19-25-16.png)

打开网页

![](pic/Snipaste_2026-09-05_19-26-44.png)

## 服务器2部署Mysql

这边参考我的安装文档：

```http
https://github.com/zzj081308/linux/blob/main/MySQL/MySQL.md
```

获取到yum仓库地址：

```http
https://dev.mysql.com/get/mysql97-community-release-el10-1.noarch.rpm
```

### 获取官方yum仓库

```shell
yum -y install https://dev.mysql.com/get/mysql97-community-release-el10-1.noarch.rpm
```

查看官方源是否启用

```shell
yum repolist enabled | grep mysql
```

![](pic/Snipaste_2026-09-06_11-11-48.png)

已经启用

### 安装Mysql服务

开始安装Mysql服务

```shell
yum -y install mysql-community-server --setopt=install_weak_deps=False
```

--setopt=install_weak_deps=False

临时修改 yum 的一个配置项：**本次安装不装“弱依赖”**

![](pic/Snipaste_2026-09-06_11-19-00.png)

### 开启服务

开启MySQL服务，并开启开机自启

检查状态

```
systemctl start mysqld
systemctl enable mysqld
systemctl status mysqld
```

设置开机自启动

![](pic/Snipaste_2026-09-06_11-22-39.png)

### 配置Mysql

寻找临时密码

```shell
cat /var/log/mysqld.log
```

![](pic/Snipaste_2026-09-06_11-29-09.png)

获取到密码

```
3fYZYNcqpu/H
```

登录mysql

```shell
mysql -uroot -p
```

![](pic/Snipaste_2026-09-06_11-31-31.png)

修改密码

```mysql
ALTER USER 'root'@'localhost' IDENTIFIED BY 'Zzj20050521@';
```

![](pic/Snipaste_2026-09-06_11-32-57.png)

至此Mysql安装完成

### 数据库创建

把项目的init.sql上传到服务器2

```shell
mysql -uroot -pZzj20050521@ < init.sql
```

执行init.sql

![](pic/Snipaste_2026-09-06_11-57-30.png)

可见数据库已经建好

## 服务器3部署Nginx

### 建nginx仓库文件

先去yum仓库新建一个nginx仓库文件

```shell
cd /etc/yum.repos.d/
vim nginx.repo
```

### 写仓库文件配置

写下如下配置文件

```nginx
[nginx-stable] 
name=nginx stable repo 
baseurl=https://nginx.org/packages/centos/$releasever/$basearch/ 
gpgcheck=1 
enabled=1 
gpgkey=https://nginx.org/keys/nginx_signing.key 
module_hotfixes=true 

[nginx-mainline] 
name=nginx mainline repo 
baseurl=https://nginx.org/packages/mainline/centos/$releasever/$basearch/ 
gpgcheck=1 
enabled=0 
gpgkey=https://nginx.org/keys/nginx_signing.key 
module_hotfixes=true
```

### 安装yum-utils组件

```shell
yum -y install yum-utils
```

![](pic/Snipaste_2026-09-06_14-17-09.png)

### 安装Nginx

开始安装Nginx

```shell
yum install -y nginx
```

![](pic/Snipaste_2026-09-06_14-18-15.png)

### 启动服务

```shell
systemctl start nginx
```

![](pic/Snipaste_2026-09-06_14-20-03.png)

### 防火墙放行

放行80端口

```shell
firewall-cmd --add-port=80/tcp --permanent
firewall-cmd --reload
```

![](pic/Snipaste_2026-09-06_14-23-32.png)

### 验证

此时在浏览器访问http://192.168.253.136:80

![](pic/Snipaste_2026-09-06_14-27-26.png)

可见welcome to nginx

### 部署前端

先把项目内frontend打包上传

![](pic/Snipaste_2026-09-06_14-35-34.png)

#### 创建前端文件夹

建立网站目录，把前端代码移动进去

```shell
mkdir -p /usr/share/nginx/html/web-demo
cp -r /root/frontend/dist/* /usr/share/nginx/html/web-demo/
ls /usr/share/nginx/html/web-demo
```

![](pic/Snipaste_2026-09-06_14-38-52.png)

#### 修改nginx配置文件

```shell
mv /etc/nginx/conf.d/default.conf /etc/nginx/conf.d/default.conf.bak
vi /etc/nginx/conf.d/web-demo.conf
```

写入如下配置

```nginx
![Snipaste_2026-09-06_14-53-33](C:\Users\27891\Desktop\运维\安装部署文档\服务器部署项目\pic\Snipaste_2026-09-06_14-53-33.png)server {
    listen 80;
    server_name 192.168.253.136;

    root /usr/share/nginx/html/web-demo;
    index index.html;

    # 单页应用路由回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # /api 反代到服务器1 后端——现在后端还没装，先注释掉，
    # 等后端上线再启用
    # location /api/ {
    #     proxy_pass http://192.168.253.128:8080;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
}
```

#### 重载nginx

```shell
nginx -t && systemctl reload nginx
```

![](pic/Snipaste_2026-09-06_14-53-33.png)

验证前端：http://192.168.253.136:80

![](pic/Snipaste_2026-09-06_14-54-51.png)

已经可见网页

## 服务器1部署后端

### 安装jdk

使用预装open  jdk

spring版本为Spring Boot 3.5.16 最高支持到jdk25

查看apt仓库jdk

```shell
apt-cache search openjdk | grep -E 'openjdk'
```

![](pic/Snipaste_2026-09-06_15-13-27.png)

可见apt仓库有jdk8/17/21/25

直接冲jdk25

```shell
apt install -y openjdk-25-jre-headless
```

![](pic/Snipaste_2026-09-06_15-15-57.png)

### 验证jdk安装

```shell
java --version
```

![](pic/Snipaste_2026-09-06_15-17-17.png)

可见jdk25已经安装成功

### 部署后端

打包上传backend文件

![](pic/Snipaste_2026-09-06_15-25-03.png)

#### 创建后端文件夹

```shell
mkdir -p /opt/web-demo
```

#### 上传jar包

```shell
![Snipaste_2026-09-06_15-41-50](C:\Users\27891\Desktop\运维\安装部署文档\服务器部署项目\pic\Snipaste_2026-09-06_15-41-50.png)cp -r /root/backend/target/web-demo.jar /opt/web-demo/
```

![](C:\Users\27891\Desktop\运维\安装部署文档\服务器部署项目\pic\Snipaste_2026-09-06_15-30-12.png)

#### 连接Mysql，运行java

远程登录暂时用不了root，先用临时用户

```shell
cd /opt/web-demo
DB_HOST=192.168.253.135 DB_USER=demo_user DB_PASSWORD='Demo@123456' java -jar web-demo.jar
```

运行发现报错，其实是Mysql防火墙没有放行3306

#### 防火墙放行3306

为了安全起见，只在服务器2放行192.168.253.128

```shell
firewall-cmd --permanent --add-rich-rule='rule family=ipv4 source address=192.168.253.128 port port=3306 protocol=tcp accept'
firewall-cmd --reload
firewall-cmd --list-all | grep 3306
```

![](pic/Snipaste_2026-09-06_15-41-50.png)

再次去服务器1运行后端

![](pic/Snipaste_2026-09-06_15-45-50.png)

运行成功！

#### 添加nginx配置

把nginx.conf里的java部分解除注释

#### 重载nginx

```shell
nginx -t && systemctl reload nginx
```

访问nginx服务器http://192.168.253.136:80

发现后端连接失败？后端连接失败：请求失败（HTTP 502)

![](pic/Snipaste_2026-09-06_15-59-52.png)

其实是Nginx联网反向代理没开

#### 开启Nginx联网反向代理

服务器3执行即可

```shell
setsebool -P httpd_can_network_connect 1
```

#### 验证

刷新网页

![](pic/Snipaste_2026-09-06_16-02-03.png)